# Core dependencies for YData Profiling GUI Tool (Updated for current versions)
ydata-profiling>=4.8.3
pandas>=2.0.0
numpy>=1.21.0

# File format support
openpyxl>=3.1.2          # Excel files (.xlsx, .xls)
xlrd>=2.0.1              # Excel files (older formats)
pyarrow>=15.0.0          # Parquet files
fastparquet>=2024.2.0    # Alternative Parquet engine

# Data processing and analysis
scipy>=1.9.0             # Scientific computing
matplotlib>=3.5.0        # Basic plotting
seaborn>=0.12.0          # Statistical visualization
plotly>=5.15.0           # Interactive plots

# HTML report generation
jinja2>=3.1.0            # Template engine for HTML
markupsafe>=2.1.0        # HTML markup safety

# Performance optimization (optional but recommended)
numba>=0.58.0            # JIT compilation
bottleneck>=1.3.0        # Fast NumPy operations

# Development and debugging (optional)
memory-profiler>=0.61.0  # Memory monitoring for large datasets

# Note: tkinter is included with Python installation
# Note: The upgrade message to ydata-sdk is just advertising, tool works fine